const initialState = {
    nicknames: []
} 

const reducer = (state = initialState, action) => {
    const { type } = action

    switch(type) {
        default:
            return state
    }
}

export default reducer